{
  "buildCommand": "node node_modules/typescript/bin/tsc -b --noCheck && node node_modules/vite/bin/vite.js build --configLoader native",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
